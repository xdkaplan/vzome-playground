name = "vzome-playground"
compatibility_date = "2026-06-01"

# Static, client-only SPA — no Worker script, just served assets.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
