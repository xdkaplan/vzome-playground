# Closed Triangles

Finds every closed triangle in the golden (icosahedral) field — three struts
whose directions sum to exactly zero — deduped by symmetry and drawn as edge
loops from the origin.

Edit the script and hit **Run**.
